// Plain (non-module) CSS imports resolve here during tests — a stylesheet has
// no behaviour worth asserting, so it becomes an empty object.
const emptyStylesheet = {};
export default emptyStylesheet;
